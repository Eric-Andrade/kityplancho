import { DataTablePipePipe } from './data-table-pipe.pipe';

describe('DataTablePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DataTablePipePipe();
    expect(pipe).toBeTruthy();
  });
});
